<%= form_with model: @model do |f| %>
<%= f.text_field :title, placeholder: "Post title here", required: true %><br>
 
 <%= f.rich_text_area :body, placeholder: "message body" ,required: true%>

  
 <%= f.submit  class: "btn btn-outline-primary mt-1" %>
 <% end %>